# Local dev: Vite proxies /api to localhost:5055 (see vite.config.ts)
# VITE_API_URL=/api

# Production (Vercel) — API proxied via vercel.json rewrites on topdealsgh.com
VITE_API_URL=/api
VITE_APP_URL=https://www.topdealsgh.com
